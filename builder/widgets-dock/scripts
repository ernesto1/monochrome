###### desktop sidebar

# ... usb-devices
cat config > ~/conky/monochrome/widgets-dock/sidebar;\
buildSidebar.bash cpu-mem network-desktop disk disk-desktop disk-maxtor temp-cpu temp-disk time-block bottom-edge;\
echo '${voffset -420}]];' >> ~/conky/monochrome/widgets-dock/sidebar




##### laptop sidebar
cat config > ~/conky/monochrome/widgets-dock/sidebar;\
buildSidebar.bash cpu-mem network-laptop disk power temp-cpu time-block bottom-edge;\
echo '${voffset -300}]];' >> ~/conky/monochrome/widgets-dock/sidebar

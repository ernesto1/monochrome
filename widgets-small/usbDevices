conky.config = {
  update_interval = 2,     -- update interval in seconds
  update_interval_on_battery = 3,
  xinerama_head = 0,       -- for multi monitor setups, select monitor to run on: 0,1,2
  total_run_times = 0,     -- this is the number of times conky will update before quitting, set to zero to run forever
  double_buffer = true,    -- use double buffering (reduces flicker, may not work for everyone)

  -- window alignment
  alignment = 'top_left',  -- top|middle|bottom_left|right
  gap_x = 2,             -- same as passing -x at command line
  gap_y = 821,

  -- window settings
  minimum_width = 169,
  minimum_height = 172,
  own_window = true,
  own_window_type = 'desktop',    -- values: desktop (background), panel (bar)
  own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',

  -- transparency configuration
  own_window_transparent = true,
  own_window_argb_visual = true,  -- turn on transparency
  own_window_argb_value = 255,    -- range from 0 (transparent) to 255 (opaque)

  -- window borders
  draw_borders = false,     -- draw borders around the conky window
  border_width = 1,         -- width of border window in pixels
  stippled_borders = 0,     -- border stippling (dashing) in pixels
  border_inner_margin = 0,  -- margin between the border and text in pixels
  border_outer_margin = 0,  -- margin between the border and the edge of the window in pixels

  -- graph settings
  draw_graph_borders = false, -- borders around the graph, ex. cpu graph, network down speed grah
                              -- does not include bars, ie. wifi strength bar, cpu bar

  imlib_cache_flush_interval = 300,
  -- use the parameter -n on ${image ..} to never cache and always update the image upon a change

  -- font settings
  draw_shades = false,    -- black shadow on text (not good if text is black)
  draw_outline = false,   -- black outline around text (not good if text is black)
  default_color = 'black',
  color1 = 'white',
  color2 = '1a336b',      -- dark blue
  color3 = '800020',      -- burgundy
};

conky.text = [[
# :::::::::::::::::::: usb devices
# :::: maxtor external drive | read 24MiB
${if_mounted /run/media/ernesto/MAXTOR}\
# disk io
${image ~/conky/monochrome/images/widgets-small/diskio.png -n -p 0,0}\
${image ~/conky/monochrome/images/widgets-small/circuitry.png -n -p 18,41}\
${voffset -4}${offset 1}${diskiograph_read /dev/sdf1 38,22 adccea 85c3ff 6000}${offset 1}${diskiograph_write /dev/sdf1 38,22 315bb1 1a336b 48000}
${voffset -36}${alignr 41}${color}${diskio_read /dev/sdf1}${offset 9}${color1}read 
${voffset 4}${alignr 41}${color}${diskio_write /dev/sdf1}${offset 9}${color1}write
# filesystem
${image ~/conky/monochrome/images/widgets-small/usbDrive.png -n -p 1, 60}\
${voffset 54}${offset 1}${color2}${if_match ${fs_used_perc /run/media/ernesto/MAXTOR} > 90}${color3}${endif}${fs_bar 3, 45 /run/media/ernesto/MAXTOR}
${voffset -32}${goto 58}${color}maxtor (${fs_type /run/media/ernesto/MAXTOR})
${voffset 4}${goto 58}${color}${fs_used /run/media/ernesto/MAXTOR} / ${fs_size /run/media/ernesto/MAXTOR}
${else}\
${voffset 93}
${endif}\
# :::: sandisk memory stick
${if_mounted /run/media/ernesto/sandisk}\
${image ~/conky/monochrome/images/widgets-small/usbStick.png -n -p 1,125}
${voffset 41}${offset 1}${color2}${if_match ${fs_used_perc /run/media/ernesto/sandisk} > 90}${color3}${endif}${fs_bar 3, 45 /run/media/ernesto/sandisk}
${voffset -49}${goto 58}${color}sandisk usb
${voffset 4}${goto 58}memory stick (${fs_type /run/media/ernesto/sandisk})
${voffset 4}${goto 58}${color}${fs_used /run/media/ernesto/sandisk} / ${fs_size /run/media/ernesto/sandisk}
${endif}\
]];

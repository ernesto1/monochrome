conky.config = {
  update_interval = 2,     -- update interval in seconds
  xinerama_head = 1,       -- for multi monitor setups, select monitor to run on: 0,1,2
  total_run_times = 0,     -- this is the number of times conky will update before quitting, set to zero to run forever
  double_buffer = true,    -- use double buffering (reduces flicker, may not work for everyone)

  -- window alignment
  alignment = 'top_left',  -- top|middle|bottom_left|right
  gap_x = 184,             -- same as passing -x at command line
  gap_y = 32,

  -- window settings
  minimum_width = 565,
  minimum_height = 230,
  own_window = true,
  own_window_type = 'desktop',    -- values: desktop (background), panel (bar)
  own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',

  -- transparency configuration
  own_window_transparent = true,
  own_window_argb_visual = true,  -- turn on transparency
  own_window_argb_value = 255,    -- range from 0 (transparent) to 255 (opaque)

  -- window borders
  draw_borders = false,     -- draw borders around the conky window
  border_width = 1,         -- width of border window in pixels
  stippled_borders = 0,     -- border stippling (dashing) in pixels
  border_inner_margin = 0,  -- margin between the border and text in pixels
  border_outer_margin = 0,  -- margin between the border and the edge of the window in pixels

  -- graph settings
  draw_graph_borders = false, -- borders around the graph, ex. cpu graph, network down speed grah
                              -- does not include bars, ie. wifi strength bar, cpu bar

  imlib_cache_flush_interval = 300,
  -- use the parameter -n on ${image ..} to never cache and always update the image upon a change
  
  top_name_verbose = true,    -- show full command in ${top ...}
  top_name_width = 30,        -- how many characters to print

  -- font settings
  draw_shades = false,    -- black shadow on text (not good if text is black)
  draw_outline = false,   -- black outline around text (not good if text is black)
  -- colors
  default_color = 'black',  -- regular text
  color1 = 'white',         -- text labels
  
  -- templates
  
  -- :: process alignment notes
  --    cpu percentage takes 5 characters while memory usage in MiB does not have a fixed width
  --    sample values: 242MiB   therefore i had to use alignr in order to align the text properly
  --                   73.2MiB  TODO use lua function to space pad this text, i'd rather work with offsets
  template0 = [[${voffset 3} ${top name \1}${top cpu \1}% ${top pid \1}${offset 20}${top_mem name \1}${alignr}${top_mem mem_res \1} ${top_mem pid \1} ]]
};

conky.text = [[
${voffset 3} ${color1}kernel${offset 9}${color}${kernel}
${voffset 3} ${color1}zoom  ${offset 9}${color}${if_running zoom}running${else}off${endif}
# :::::::::::::::::::: processes
${image ~/conky/monochrome/images/widgets-small/table-processes-cpu.png -n -p 0,59}\
${image ~/conky/monochrome/images/widgets-small/table-processes-memory.png -n -p 284,59}\
${voffset 27} process${goto 217}cpu   pid${offset 20}process${alignr}mem   pid ${voffset 3}
${template0 1}
${template0 2}
${template0 3}
${template0 4}
${template0 5}
${template0 6}
${template0 7}
${template0 8}
#${voffset 8}${offset 50}${cpubar cpu0 3,105}
]];
